# Root level ignores
.env
*.env.local
*.log

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Dependencies
node_modules/

# Docker override
docker-compose.override.yml

# graphify: fully local/regenerable knowledge graph — never commit it.
graphify-out/

# Local tooling / assistant config (skills, settings, memory)
.claude/

# Docs / plans / specs kept local, not in the repo
docs/

# Credentials & secrets — never commit any real env or key material
.env
.env.*
!.env.example
*.env
*.pem
*.key
*.p12
*.pfx
id_rsa
*.htpasswd
secrets.*
*credentials*
*secret*.json
